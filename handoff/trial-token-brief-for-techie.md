# Бриф технарю — TRY-токены после оплаты (Prodamus) + @AITeamVIPBot

## Задача

Бот `@AITeamVIPBot` должен выдавать **TRY-токены ПОСЛЕ успешной оплаты**
через **Prodamus** (платный вход в тест-драйв). Это новый тариф `TRY`
рядом с существующими `VIP / STD / SUB / HRM`.

Цель: тест-драйв даётся только заплатившим (квалифицирует лида). Ссылка
на оплату — на сайте; токен прилетает в Telegram после оплаты.

## Хорошая новость: ключ и механизм — те же

TRY использует **тот же Ed25519-ключ**, что VIP/STD/SUB/HRM. Менять
ключи и публичный ключ в установщике НЕ нужно. Отличие только в payload
(новый префикс `TRY`).

## Формат токена

```
TRY-<hash16>-<tg_id>-<подпись_b64url>
```

- `hash16` — 16 символов `[A-F0-9]` (UPPERCASE hex). Привяжи к платежу:
  например первые 16 hex от `sha256(prodamus_order_id)` (UPPERCASE), или
  просто `crypto.randomBytes(8).hex().toUpperCase()`. Это opaque-id для
  логов/сверки с оплатой; безопасность даёт подпись.
- `tg_id` — Telegram user_id покупателя (5–15 цифр). **Обязателен** — в
  токен зашит TG, установщик ставит на него `allowFrom` (анти-шаринг).
- `подпись_b64url` — Ed25519-подпись (base64url **без padding**, ~86 симв).

## Что подписывать

Payload (UTF-8, ровно так, с разделителем `|`):

```
TRY|<hash16>|<tg_id>
```

Подпись тем же приватным ключом, что для остальных тарифов.

## Код выдачи (Node, как для других тарифов)

```js
const crypto = require('crypto');

function makeTryToken(tgId, orderId, privateKeyPem) {
  const hash16 = crypto.createHash('sha256')
    .update(String(orderId)).digest('hex').slice(0, 16).toUpperCase();
  const payload = `TRY|${hash16}|${tgId}`;
  const sig = crypto.sign(
    null,
    Buffer.from(payload, 'utf8'),
    crypto.createPrivateKey(privateKeyPem)
  );
  return `TRY-${hash16}-${tgId}-${sig.toString('base64url')}`;
}
```

Установщик проверяет это так (для справки, менять не нужно):

```js
crypto.verify(null, Buffer.from(`TRY|${hash16}|${tgId}`), PUBLIC_KEY,
              Buffer.from(sigB64, 'base64url'));
```

## Поток оплаты (Prodamus) → токен

⚠️ **Ключевой момент:** в токен нужен `tg_id` покупателя. Значит платёж
надо связать с его Telegram. Рекомендую так (ссылка на сайте остаётся):

**Вариант A — через бота (рекомендую, надёжно):**
1. На сайте кнопка «Оплатить» → ведёт в бота (`t.me/AITeamVIPBot?start=buy`)
   ИЛИ бот доступен прямо с сайта.
2. В боте кнопка «Оплатить тест-драйв» → бот знает `tg_id`, генерит
   **персональную Prodamus-ссылку** (кладём `tg_id`/`order_id` в кастомное
   поле Prodamus) и шлёт её пользователю.
3. Клиент платит на Prodamus.
4. Prodamus шлёт **webhook** на наш сервер (URL уведомления в настройках
   Prodamus). По webhook проверяем статус «оплачено», достаём `tg_id` из
   кастомного поля.
5. `token = makeTryToken(tg_id, order_id, PRIVATE_KEY_PEM)` → бот **DM-ит**
   пользователю готовую команду установки.

**Вариант B — чистая ссылка на сайте без бота:**
- Тогда после оплаты Prodamus `success_url` → редирект на
  `t.me/AITeamVIPBot?start=paid_<order_id>`. Клиент открывает бота (тут
  бот узнаёт его `tg_id`), бот сверяет `order_id` с webhook-ом (оплачен ли)
  и выдаёт токен. Тоже рабочее; чуть больше шагов для клиента.

В обоих вариантах токен в итоге выдаёт бот (у него приватный ключ),
`tg_id` известен — биндинг сохраняется.

## Сообщение пользователю после оплаты

```
Оплата прошла ✅ Твой доступ к тест-драйву AI TEAM 2.0 👇

macOS / Linux — вставь в Терминал:

bash <(curl -fsSL https://raw.githubusercontent.com/tonytrue92-beep/openclaw-test-drive/main/scripts/install-trial.sh) --token TRY-XXXX...
```

(подставь реальный токен вместо `TRY-XXXX...`).

## Проверки/нюансы

- **Только после оплаты.** Токен генерим строго по подтверждённому
  webhook-у Prodamus (статус «оплачено»/`success`), не по факту перехода
  на success_url (его можно подделать).
- **Длина подписи:** Ed25519 = 64 байта → base64url без padding = 86
  символов. Установщик принимает 80–100 — ок.
- **hash16 строго UPPERCASE hex**, ровно 16 символов, иначе формат-гейт
  не примет.
- **tg_id 5–15 цифр**, реального аккаунта покупателя — в токен зашит TG,
  установщик ставит на него `allowFrom` (бот ответит только ему).
- Сеть для проверки токена установщику НЕ нужна (проверка локальная
  публичным ключом). Бот может лежать — уже выданные токены работают.
- **Отзыв:** проверка локальная, мгновенного отзыва нет (как и у платных).
  Если нужен — отдельная история (revocation-list endpoint).

## Лимиты / антифрод (на твоё усмотрение)

- 1 активный TRY на оплаченный `order_id`.
- Идемпотентность webhook (Prodamus может прислать повтор) — не выдавать
  два токена за один платёж.
- Проверять подпись webhook от Prodamus (у них есть HMAC-подпись) — чтобы
  никто не подделал «оплачено».
